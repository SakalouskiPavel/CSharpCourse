# Вопросы — урок 29

1. Почему удалённый вызов нельзя считать обычным вызовом метода?
2. Что должен определять сервисный контракт?
3. Чем RPC, message style и resource style различаются по главному объекту моделирования?
4. Почему JSON сам по себе не делает API RESTful?
5. Как связаны XML и XSD?
6. Для чего SOAP нужны Envelope, Header, Body и Fault?
7. Что описывает WSDL и зачем по нему генерируют proxy?
8. Что такое ASMX и почему его относят к legacy?
9. Расшифруйте Address, Binding и Contract в WCF.
10. Чем service, data и message contracts отличаются?
11. Зачем одному WCF-сервису несколько endpoints/bindings?
12. Каково положение server WCF, WCF client libraries и CoreWCF в современном .NET?
13. Почему перенос на CoreWCF требует инвентаризации, а не только recompilation?
14. В каких условиях сохранение SOAP разумно?
15. Какие стадии проходит запрос в ASP.NET Core pipeline?
16. Что делают model binding и validation?
17. Что в REST означает stateless и чего это не запрещает?
18. Что такое ресурс и почему URI не обязан повторять таблицы БД?
19. В чём различаются `POST`, `PUT` и `PATCH`?
20. Чем safe-метод отличается от idempotent?
21. Почему повторный `DELETE` может вернуть другой статус и остаться идемпотентным?
22. Когда уместны `200`, `201`, `202` и `204`?
23. Чем `400`, `409`, `412` и `422` выражают разные ошибки?
24. Почему `{ success:false }` с `200` ухудшает контракт?
25. Зачем отделять DTO от domain entity?
26. Где проверяется форма DTO, а где бизнес-инвариант?
27. Какие стандартные поля содержит Problem Details?
28. Почему stack trace не должен попадать клиенту?
29. Как `traceId` связывает ответ и диагностику?
30. Как возникает повтор неидемпотентного запроса?
31. Что должен гарантировать механизм `Idempotency-Key`?
32. Почему неатомарная проверка idempotency key оставляет гонку?
33. Как совместно работают ETag и `If-Match`?
34. Когда возвращают `304`, а когда `412`?
35. Почему collection endpoint требует ограничения размера?
36. Чем offset pagination отличается от cursor pagination?
37. Зачем сортировке стабильный tie-breaker?
38. Что описывает OpenAPI и чего он не гарантирует автоматически?
39. Какие изменения API обычно breaking?
40. Что такое additive evolution и tolerant reader?
41. Какие шаги нужны при прекращении старой версии?
42. Что делает anti-corruption layer при миграции SOAP?

## Ситуации

43. Клиент дважды отправил создание заказа после timeout. Спроектируйте безопасное поведение.
44. Два оператора редактируют заказ версии 7. Как ETag предотвращает потерю изменений?
45. Legacy WCF использует нестандартный binding. Какие сведения надо собрать до выбора CoreWCF?
46. Endpoint возвращает миллион заказов. Какие элементы контракта нужно добавить?
47. Команда хочет заменить SOAP на REST за один релиз. Назовите основные риски и более безопасный путь.
