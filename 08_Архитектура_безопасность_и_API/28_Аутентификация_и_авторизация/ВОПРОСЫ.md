# Вопросы — урок 28

Ответьте своими словами. Если ответ превращается в одно определение, добавьте пример из сервиса заказов.

1. Чем актив отличается от угрозы?
2. Почему HTTPS не устраняет ошибки авторизации?
3. Чем идентификатор отличается от credential?
4. Как различаются аутентификация и авторизация?
5. Что представляют identity, claim, issuer и principal?
6. Почему claim ещё не является разрешением?
7. Почему нельзя хранить пароль открытым текстом или обратимо зашифрованным?
8. Почему быстрый SHA-256 сам по себе не подходит для password hashing?
9. Для чего нужны уникальная соль и настраиваемая стоимость hashing?
10. Какие меры ограничивают перебор паролей?
11. Какие два способа хранения состояния сессии описаны в теории?
12. Что дают `Secure`, `HttpOnly` и `SameSite`?
13. Почему `HttpOnly` не устраняет весь ущерб XSS?
14. За счёт какого свойства cookie возникает CSRF?
15. Чем CSRF отличается от XSS?
16. Почему CORS не является аутентификацией и не блокирует серверных клиентов?
17. Что означает bearer в bearer token?
18. Чем opaque token отличается от самодостаточного?
19. Из каких частей обычно состоит JWT?
20. Почему подписанный JWT нельзя считать зашифрованным?
21. Какие параметры JWT обязан проверить API?
22. Как различаются OAuth 2.0 и OpenID Connect?
23. Какова роль authorization server, client и resource server?
24. Зачем Authorization Code flow использует PKCE?
25. Почему приложение не должно заново проектировать password/implicit flow?
26. Чем различаются access token, ID token и refresh token?
27. Почему refresh token требует более строгой защиты?
28. Что такое rotation и reuse detection refresh token?
29. Почему logout не всегда немедленно отзывает access token?
30. Что в ASP.NET Core называется authentication scheme и handler?
31. Чем `Challenge` отличается от `Forbid`?
32. Когда ответ уместно вернуть `401`, а когда `403`?
33. Зачем иногда вместо `403` возвращают `404`?
34. Чем role отличается от permission и policy?
35. Что даёт fallback policy?
36. Что означает resource-based authorization?
37. Где должна выполняться проверка владельца заказа и почему UI недостаточно?
38. Что означает принцип least privilege?
39. Чем service identity отличается от пользовательской?
40. Где нельзя хранить secrets и что делать после их утечки в Git?
41. Чем audit отличается от диагностического лога?
42. Какие сведения полезны в audit-событии и какие туда помещать нельзя?

## Ситуации

43. Пользователь вошёл, но пытается отменить чужой заказ. Какой результат и какая проверка нужны?
44. API декодирует JWT, но не проверяет подпись и `aud`. Почему это критическая ошибка?
45. SPA отправляет ID token в orders API. Объясните ошибку и исправление.
46. Разработчик добавил `Access-Control-Allow-Origin: *` и назвал API защищённым. Что осталось незащищённым?
47. В логе появился bearer token. Какие действия нужны кроме удаления строки из кода?
