# Вопросы — урок 14

1. Какую проблему решают generics по сравнению с дублированием и object?
2. Что такое type parameter и type argument?
3. Чем open generic type отличается от closed constructed type?
4. Где можно использовать T внутри generic type?
5. Как называть несколько type parameters?
6. Как compiler выполняет type inference?
7. Почему return type обычно недостаточен для inference?
8. Почему без constraint нельзя вызвать произвольный member/operator на T?
9. Что означают class, struct, notnull и unmanaged constraints?
10. Что дают base class и interface constraints?
11. Что гарантирует new() и почему он не заменяет factory/DI?
12. Каков порядок нескольких constraints?
13. Когда generic interface полезен?
14. Почему универсальный repository может скрывать семантику?
15. Чему равен default(T) для reference/value types?
16. Почему default не всегда подходит для «не найдено»?
17. Является ли notnull runtime guard?
18. Что такое boxing/unboxing?
19. Почему `List<int>` не boxing-ит каждый элемент как object collection?
20. Что означает reified generics в .NET?
21. Почему List<Cat> нельзя присвоить List<Animal>?
22. Что разрешает `out T`?
23. Что разрешает `in T`?
24. Где variance доступна и почему ограничена reference conversions?
25. Как generic overload участвует в overload resolution?
26. Что дают static abstract interface members?
27. Для чего INumber<T>?
28. Какие признаки показывают избыточную generic abstraction?

## Ситуации

29. Метод содержит `if (typeof(T) == ...)` для пяти типов. Что это говорит об универсальности?
30. API возвращает default(T) при отсутствии, а T может быть int. Как убрать неоднозначность?
31. Factory<T> требует new(), но объекты нуждаются в dependencies. Как изменить construction?
32. Требуется только читать кошек как животных. Какой variance contract подойдёт?

Готовность: для generic API назовите одинаковый алгоритм, capabilities T и случаи, которые должны быть запрещены compiler-ом.
