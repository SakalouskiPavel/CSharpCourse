# Вопросы — урок 05

## Основы

1. Что обозначает `<T>` в `List<T>`?
2. Чем массив отличается от `List<T>`?
3. Почему первый индекс равен нулю и как найти последний?
4. Когда выбирать `for`, а когда `foreach`?
5. Почему нельзя обычно изменять структуру коллекции внутри `foreach`?
6. Чем `Count` отличается от `Capacity`?
7. Почему добавление в конец списка называют амортизированным O(1)?
8. Почему вставка в начало списка имеет O(n)?

## Сложность

9. Что описывает Big O и чего она не сообщает?
10. Приведите пример O(1), O(n) и O(log n).
11. Почему O(n) не всегда означает плохое решение?

## Специализированные коллекции

12. Что хранит `Dictionary<TKey,TValue>`?
13. Чем `Add` словаря отличается от присваивания через индексатор?
14. Почему для неизвестного ключа удобен `TryGetValue`?
15. Как связаны `Equals` и `GetHashCode`?
16. Почему нельзя изменять значимую часть ключа внутри словаря?
17. Для чего нужен `IEqualityComparer<T>` или `StringComparer`?
18. Когда `HashSet<T>` лучше списка?
19. Какие операции множеств поддерживает HashSet?
20. Чем FIFO отличается от LIFO? Какие коллекции их реализуют?
21. Почему `LinkedList<T>` не обеспечивает быстрый поиск середины?
22. Когда нужна sorted collection и какова цена поддержки порядка?

## Контракты и изменяемость

23. Чем `IEnumerable<T>` отличается от `IList<T>`?
24. Почему метод печати лучше принять `IEnumerable<T>`, а не `List<T>`?
25. Почему `IReadOnlyList<T>` не гарантирует неизменяемость данных?
26. Как ведёт себя операция Add у immutable collection?
27. Делает ли immutable collection изменяемый объект внутри неизменяемым?
28. Для чего нужны concurrent collections?
29. Почему `ContainsKey` плюс присваивание не становятся одной атомарной операцией?

## Выбор

30. Подберите коллекцию: календарь из 12 месяцев; очередь печати; уникальные роли; товар по артикулу; история отмены; изменяемый список покупок.
31. Что нужно узнать о задаче до выбора коллекции?

Готовность: для каждого выбора назовите главную операцию, ожидаемую сложность и правило данных, которое коллекция выражает.
