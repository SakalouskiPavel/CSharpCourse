# Вопросы — Урок 05. Особенности встроенных коллекций

Сначала ответьте без конспекта. Для каждого ответа приведите короткий пример или контрпример.

1. Почему добавление в `List<T>` называют амортизированным O(1)?
2. Чем capacity отличается от count?
3. Какие требования связывают `Equals` и `GetHashCode`?
4. Когда `HashSet<T>` лучше `List<T>`?
5. Почему `IReadOnlyList<T>` не гарантирует неизменяемость?
6. Когда выбрать `SortedDictionary`?
7. Что даёт immutable collection?
8. Почему две операции ConcurrentDictionary могут образовать race condition?
9. Как comparer влияет на уникальность?

## Практика объяснения

Выберите два вопроса и объясните их так, будто собеседник знает синтаксис C#, но не знаком с темой. Затем назовите одно распространённое заблуждение и способ проверить ответ маленькой программой.

