# Вопросы — урок 06

1. Что означает LINQ и какую задачу решает?
2. Как читать `book => book.Price >= 50m`?
3. Когда именованный метод лучше лямбды?
4. Что обещает `IEnumerable<T>` и чего не обещает?
5. Что значит отложенное выполнение?
6. Какие действия обычно запускают перечисление?
7. Что меняет `ToList` и клонирует ли он объекты?
8. Чем `Where` отличается от `Select`?
9. Когда нужен `SelectMany`?
10. Чем `First`, `FirstOrDefault`, `Single` и `SingleOrDefault` выражают разные ожидания?
11. Почему `First` не проверяет уникальность?
12. Чем `Any()` лучше `Count() > 0` для произвольного потока?
13. Почему `All` возвращает true для пустой последовательности?
14. Чем `ThenBy` отличается от второго `OrderBy`?
15. Какова общая цена сортировки и почему она не потоковая с первого элемента?
16. Как ведут себя `Average`, `Min`, `Max` на пустом наборе?
17. Для чего нужен `Aggregate` и когда готовый оператор яснее?
18. Как comparer влияет на `Distinct`?
19. Что возвращает `GroupBy`?
20. Когда anonymous type уместен, а когда нужен именованный DTO/class?
21. Какой результат даёт `Join` для элемента без пары?
22. Чем method syntax отличается от query syntax на уровне выполнения?
23. Как работает `yield return`?
24. Почему повторное перечисление может повторить I/O или side effects?
25. Когда материализация полезна, а когда вредна?
26. Чем `IQueryable<T>` отличается от `IEnumerable<T>`?
27. Почему не вся C#-логика переводится в SQL?

## Прогноз

28. Запрос создан, затем в исходный список добавлен элемент, потом выполнен `foreach`. Увидит ли deferred query элемент?
29. Что изменится, если `ToList` вызвать до добавления?
30. Почему `Select(book => book.Tags)` и `SelectMany(book => book.Tags)` имеют разную форму?
31. Найдите риск: `query.Count(); query.Sum(...); query.ToList()` для источника из файла.

Готовность: для любого запроса отметьте источник, тип элемента после каждого оператора и место фактического выполнения.
