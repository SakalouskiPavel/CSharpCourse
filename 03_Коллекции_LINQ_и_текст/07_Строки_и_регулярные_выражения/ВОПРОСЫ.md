# Вопросы — урок 07

## Строки и Unicode

1. Чем `string` отличается от `char`?
2. Чем отличаются `null`, пустая строка и whitespace-only строка?
3. Что означает неизменяемость string?
4. Почему серия `+=` в большом цикле создаёт лишнюю работу?
5. Чем обычная, verbatim и raw string различаются?
6. Что такое code point, UTF-16 code unit и grapheme cluster?
7. Почему `"😀".Length` равна 2?
8. Когда использовать `Rune`, а когда `StringInfo`?
9. Зачем нужна Unicode normalization и чего она не делает?

## Сравнение и форматирование

10. Почему правило сравнения зависит от цели текста?
11. Когда выбирать `Ordinal` и `OrdinalIgnoreCase`?
12. Когда нужна пользовательская CultureInfo?
13. Почему `ToLower() ==` хуже явного StringComparison?
14. Какие операции дают `Trim`, `StartsWith`, `Contains`, `Replace`, `Split` и `Join`?
15. Почему индексы и диапазоны могут разорвать пользовательский символ?
16. Чем формат для человека отличается от машинного формата?
17. Почему нельзя вручную заменять запятую на точку перед разбором числа?
18. Когда StringBuilder оправдан, а когда избыточен?
19. Что даёт Span и какие ограничения имеет?

## Регулярные выражения

20. Для каких задач regex подходит и для каких нет?
21. Разберите `\A[A-Z]{2}-[0-9]{4}\z` по элементам.
22. Почему `\d` не всегда равно `[0-9]`?
23. Чем C# escaping отличается от regex escaping?
24. Что обозначают `?`, `*`, `+`, `{m,n}`?
25. Чем greedy квантификатор отличается от lazy?
26. Чем capturing, non-capturing и named groups различаются?
27. Почему нужно проверить `Match.Success` перед чтением групп?
28. Чем `^/$` отличаются от `\A/\z` в multiline mode?
29. Что делает lookaround?
30. Что такое backtracking и ReDoS?
31. Какие меры защищают обработку недоверенного ввода?
32. Что даёт GeneratedRegex и чего он не исправляет?
33. Почему совпадение шаблона даты не доказывает существование даты?

## Ситуации

34. Для проверки префикса `ID-` предложен regex. Какой вариант проще?
35. Логин сравнивается через `CurrentCultureIgnoreCase`. Какой риск и какая альтернатива?
36. Regex без anchors нашёл допустимый фрагмент внутри мусорной строки. Почему это не полная валидация?
37. Пользователь отправил миллион символов в поле с regex. Какие границы должны существовать до сопоставления?

Готовность: для текстовой задачи сначала назовите семантику сравнения, формат, доверие к input и только затем выбирайте string API или regex.
