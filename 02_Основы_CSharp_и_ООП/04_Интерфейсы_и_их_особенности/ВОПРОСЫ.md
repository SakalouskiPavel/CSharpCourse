# Вопросы — урок 04

1. Какую проблему создаёт самостоятельное `new EmailSender()` внутри сервиса?
2. Что обещает интерфейс и чего он не сообщает о реализации?
3. Почему нельзя создать `new INotificationSender()`?
4. Что видно через переменную интерфейсного типа?
5. Где следует выбирать конкретную реализацию зависимости?
6. Как constructor injection делает зависимость явной?
7. Может ли класс реализовать несколько интерфейсов? Зачем это нужно?
8. Сформулируйте Interface Segregation Principle своими словами.
9. Почему интерфейс с `Send`, `AttachFile` и `Schedule` может быть плохим для SMS?
10. Когда интерфейс лучше abstract class?
11. Когда abstract class лучше интерфейса?
12. Почему не каждому классу нужен интерфейс?
13. Что такое явная реализация и как вызвать такой член?
14. Когда конфликт имён оправдывает явную реализацию?
15. Что даёт default interface member и какую сложность вводит?
16. Чем static abstract member отличается от обычного instance member?
17. Почему `IEnumerable<Cat>` можно читать как `IEnumerable<Animal>`?
18. Почему `IList<Cat>` нельзя безопасно считать `IList<Animal>`?
19. Что означают `out T` и `in T` в объявлениях generic-интерфейсов?
20. Чем fake, stub и mock различаются по назначению?
21. Почему интерфейс не следует создавать только ради mock framework?

## Ситуации

22. Клиент использует только `Read`, но зависит от интерфейса с двадцатью командами чтения и записи. Какой принцип подсказывает изменение?
23. Две реализации имеют одинаковую сигнатуру, но одна считает «отправлено» после постановки в очередь, другая — после доставки. Достаточно ли общего интерфейса без уточнения семантики?
24. В коде повсюду вызывается `ServiceLocator.Get<T>()`. Почему зависимости остаются скрытыми?
25. Нужно обработать только чтение животных. Какая variance безопасна и почему?

Готовность: нарисуйте клиента, интерфейс, две реализации и composition root; стрелками покажите, кто о ком знает.
