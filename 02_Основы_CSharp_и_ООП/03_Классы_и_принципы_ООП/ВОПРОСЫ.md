# Вопросы — урок 03

## Класс и объект

1. Чем класс отличается от объекта?
2. Что хранит переменная ссылочного типа?
3. Что делают `new` и конструктор?
4. Чем состояние отличается от поведения?
5. Чем поле отличается от свойства?
6. Что разрешают `get`, `set` и `private set`?
7. Когда свойству нужно собственное backing field?
8. Почему обязательные данные лучше проверять в конструкторе?
9. Что делает `: this(...)` между конструкторами?

## Корректность и доступ

10. Что такое инвариант? Назовите четыре инварианта учебного счёта.
11. Почему проверка только в консольном интерфейсе не защищает объект?
12. Чем `public`, `private`, `protected` и `internal` отличаются на базовом уровне?
13. Почему публичный API желательно делать минимальным?
14. Чем инкапсуляция отличается от механического сокрытия полей?
15. Что такое абстракция? Как метод `TryWithdraw` скрывает детали?

## Ссылки и виды членов

16. Почему изменение через `second` видно через `first`, если выполнено `second = first`?
17. Что означает `null` и когда возникает `NullReferenceException`?
18. Зачем nullable-аннотация `?`?
19. Чем static member отличается от instance member?
20. Какие проблемы создаёт изменяемое static state?

## Наследование, композиция и полиморфизм

21. Какое отношение должно выражать наследование?
22. Что означают `abstract`, `virtual`, `override` и `sealed`?
23. Что такое подстановка базового типа потомком?
24. Чем динамический полиморфизм отличается от перегрузки?
25. Что означает композиция?
26. Почему принтер выписки не должен быть базовым классом счёта?
27. Когда композиция удобнее наследования?

## Выбор типа и зависимости

28. Когда выбрать class, struct или record?
29. Почему изменяемая структура может вести себя неожиданно?
30. Что означает равенство record по значениям?
31. Делает ли record вложенную коллекцию неизменяемой?
32. Что такое зависимость объекта?
33. Почему обязательные зависимости передают через конструктор?

## Ситуации

34. У `BankAccount.Balance` публичный setter. Какой дефект проектирования это создаёт?
35. `EmailReport : BankAccount` выбран ради повторного использования метода форматирования. Почему связь неверна?
36. Объект заказа сначала создают пустым, затем вызывают пять setters в обязательном порядке. Как улучшить контракт?
37. Два `Address` с одинаковыми данными должны считаться одинаковыми. Какой вид типа стоит рассмотреть?

Готовность: нарисуйте объект, его публичную границу, закрытое состояние и инварианты. Объясните, через какие операции состояние может измениться.
