# Вопросы — урок 11

1. Чем runtime exception отличается от compile и logic error?
2. Что происходит со стеком вызовов после throw?
3. Что содержат Message, StackTrace и InnerException?
4. В каком порядке выбираются catch blocks?
5. Когда выполняется finally?
6. Почему try должен охватывать понятную операцию?
7. Как выбрать между Try/result и exception?
8. Почему неверный пользовательский ввод обычно не исключителен?
9. Когда применять ArgumentNullException, ArgumentOutOfRangeException и InvalidOperationException?
10. Что означает «обработать исключение»?
11. Почему пустой catch опасен?
12. Чем `throw;` отличается от `throw ex;`?
13. Когда оправдана обёртка с InnerException?
14. Что даёт exception filter?
15. Когда нужен custom exception?
16. Почему context лучше хранить property, а не разбирать Message?
17. Как using связан с try/finally?
18. Где находится exception async operation до await?
19. Почему async void опасен?
20. Что происходит, если несколько tasks в WhenAll завершаются ошибкой?
21. Почему cancellation имеет отдельную семантику?
22. Где логировать исключение и почему не на каждом слое?
23. Почему в logging API передают exception object, а не только Message?
24. Что должна делать верхняя граница console/Web app?
25. Почему retry небезопасен для любой операции?
26. Можно ли продолжать использовать объект после любого исключения?

## Ситуации

27. Метод поиска не нашёл необязательную книгу. Нужен ли exception?
28. Catch пишет лог и делает `throw ex`. Какие две проблемы?
29. Пользователь отменил загрузку, а система пишет critical error. Что потеряно?
30. Save order автоматически повторяется после timeout. Какой вопрос задать об idempotency?
31. `catch (Exception) { return null; }` скрывает file/json/database errors. Как перепроектировать контракт?

Готовность: для пяти ошибок назовите тип исхода, место обработки, cleanup, logging и возможность retry.
