# Вопросы — урок 08

1. Что такое assembly и какие данные она содержит?
2. Чем IL отличается от metadata?
3. Что описывает объект `Type`?
4. Чем `typeof(T)`, `obj.GetType()` и `Type.GetType(string)` различаются?
5. Почему нельзя вызвать `GetType()` у null?
6. Что проверяет `IsAssignableFrom`?
7. Какие виды `MemberInfo` существуют?
8. Что по умолчанию возвращает `GetProperties()`?
9. Для чего нужны BindingFlags?
10. Почему неполный набор flags может вернуть пустой результат?
11. Что возвращает `PropertyInfo.GetValue` и почему тип — object?
12. Какие проверки нужны перед `SetValue`?
13. Какие compile-time проверки теряются при `MethodInfo.Invoke`?
14. Где искать исходное исключение вызванного метода?
15. Как работает `Activator.CreateInstance`?
16. Что проверять перед созданием plugin type?
17. Чем open generic type отличается от closed generic type?
18. Что делает `MakeGenericType`?
19. Какие этапы разделяют discovery, validation, activation и execution?
20. Почему после создания handler лучше вызывать через интерфейс?
21. Почему Reflection дороже прямого вызова?
22. Что стоит кэшировать?
23. Почему нельзя создавать произвольный тип из пользовательской строки?
24. Даёт ли interface изоляцию недоверенного plugin code?
25. Для чего нужен AssemblyLoadContext?
26. Почему collectible context иногда не выгружается?
27. Почему trimming конфликтует с динамическим поиском членов?
28. Когда source generator лучше Reflection?

## Ситуации

29. `GetMethod("Run")` находит несколько перегрузок. Как уточнить поиск?
30. Сканирование всех assemblies выполняется на каждый HTTP-запрос. Как изменить lifecycle?
31. Плагин загружен в отдельном AssemblyLoadContext, но сохранён в static list. Почему выгрузка не произойдёт?
32. Сериализатор читает свойства динамически. Какие публикационные режимы требуют особого внимания?

Готовность: объясните границу между информацией, известной компилятору, и проверками, перенесёнными в runtime.
