# Практика — Урок 12. Работа с SQL и NoSQL базами данных

## Задача

Создать хранилище каталога на EF Core или ADO.NET с CRUD, поиском, транзакцией и защитой от конкурентного обновления.

## Обязательные требования

- использовать локальную БД/контейнер и воспроизводимую схему;
- все пользовательские значения передавать параметрами;
- добавить constraints и индекс под выбранный запрос;
- реализовать короткую транзакцию из нескольких изменений;
- покрыть integration tests реальной БД;

## Порядок работы

1. Запишите входы, выходы, ошибки и ограничения.
2. Создайте отдельные production- и test-проекты в solution.
3. Реализуйте минимальный сквозной сценарий и зафиксируйте коммитом.
4. Добавьте граничные случаи, диагностику и документацию запуска.
5. Выполните `dotnet build` и `dotnet test`.

## Критерии готовности

- обязательные сценарии воспроизводятся по README;
- выбранные конструкции усиливают контракт, а не маскируют дизайн;
- тесты проверяют успех, границы и ожидаемые ошибки;
- ресурсы, данные и исключения обрабатываются предсказуемо;
- ученик может объяснить альтернативу каждому нетривиальному решению.

## Дополнительный уровень

- сравнить generated SQL двух вариантов LINQ-запроса;
- реализовать тот же read model в document-oriented форме и описать компромисс;

